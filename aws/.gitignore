# Terraform local artifacts (applies to aws/ and aws/bootstrap/).
# Commit .terraform.lock.hcl — it is NOT ignored here.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
