# Non-secret bootstrap inputs. No credentials here (OIDC only) — safe to commit.
aws_region        = "eu-central-1"
github_repo       = "miveal/mojerodos-infra"
prod_environment  = "prod"
state_bucket_name = "mojerodos-tfstate"
deploy_role_name  = "mojerodos-cicd-deploy"
